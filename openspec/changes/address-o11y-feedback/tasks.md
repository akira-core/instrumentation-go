# Tasks: address-o11y-feedback

## 1. otel-nats — F1 HeaderCarrier reads

- [x] 1.1 Add failing tests in `otelnats`: `Values` returns all values for a multi-instance `baggage` header; `Get`/`Values` fall back to `textproto.CanonicalMIMEHeaderKey` form; verbatim key wins when both forms exist; `Set` still writes verbatim
- [x] 1.2 Implement `HeaderCarrier.Values` + canonical fallback in `Get` (`otelnats/propagation.go`); add `var _ propagation.ValuesGetter` compile-time assertion
- [x] 1.3 Run `go build ./... && go test -race ./... && golangci-lint run ./...` in `otel-nats/` — all green

## 2. otel-nats — F4 batch Stop promptness

- [x] 2.1 Add failing test: `Stop()` unblocks a batch whose forwarding goroutine is parked on an empty native batch (no message ever arrives), bounded wait, for both direct and traced paths
- [x] 2.2 Rewrite forwarding loops in `newDirectMessageBatch` and `newTracedMessageBatch` (`oteljetstream/consumer.go`) to the nested-select shape (observe `done` on receive and send)

## 3. otel-nats — F3 Consumer.Next live cancellation

- [x] 3.1 Add failing tests: cancelling a deadline-less ctx mid-wait returns `ctx.Err()` promptly with no leaked goroutine; message delivered before cancel behaves exactly as today (span, returned ctx)
- [x] 3.2 **Design correction during implementation**: `jetstream.MessageBatch` (upstream) has no `Stop()` method — that was a misread of `MessagesContext`/`ConsumeContext`'s `Stop()` during design. Upstream `Fetch`/`Next` already select on `ctx.Done()` internally when given `jetstream.FetchContext(ctx)` (which also derives server-side expiry from the ctx deadline with its own buffer logic), so no manual `Stop()`/drain/`Nak()` is needed or possible — reimplemented `applyCtxDeadlineToFetchOpts` as `applyCtxToFetchOpts`, replacing the deadline→`FetchMaxWait` conversion with `jetstream.FetchContext(ctx)`. Guards on `ctx.Done() == nil` (not just `ctx == nil`) so `context.Background()` combined with a caller-supplied `FetchMaxWait` keeps working exactly as before (`FetchContext`+`FetchMaxWait` together is a native error) — caught via `TestOrderedConsumerNamePrefixAttr` regressing.
- [x] 3.3 Applied the same `applyCtxToFetchOpts` shape to both `directConsumer.Next` and `tracedConsumer.Next` (they already shared the one helper)

## 4. otel-nats — R5 receive spans end at handover

- [x] 4.1 Add failing tests: batch message span already ended (`IsRecording() == false`) when received from `Messages()`; `MessagesContext.Next` span ended at return
- [x] 4.2 Traced batch forwarder: end each span right after `tracer.Start` (handover, before the channel send); delete `lastSpan` bookkeeping in `oteljetstream/consumer.go`
- [x] 4.3 `tracedMessagesContext.Next`: end the span before returning; removed `lastSpan`, `mu`, `stopped` fields entirely (no longer needed — no cross-call span state means nothing for concurrent `Stop`/`Drain` to race against); updated doc comments

## 5. otel-nats — F2 semconv consumer-group key

- [x] 5.1 Replaced the `attrConsumerName` string constant with `semconv.MessagingConsumerGroupNameKey.String(...)` directly in `receiveBaseAttrs` (`oteljetstream/consumer.go`) — the single call site used by both pull (`receive`) and push (`process`) consumer spans, including the ordered-consumer fallback path (`orderedConsumerNameFromConfig` in `jetstream.go` only supplies the name *value*, unchanged); updated all 5 test assertion sites (`consumer_test.go` ×3, `pushconsumer_test.go` ×2) plus the Docker-gated `tests/integration/jetstream_test.go`
- [x] 5.2 Verified via `grep -rn "messaging.consumer.name\b" otel-nats/` excluding `.group.name` matches: only `README.md`/`README.zh-TW.md` remain (deferred to task 9, docs) and the explanatory comment in `consumer.go` referencing the old string for migration context

## 6. otel-nats — R1 WithTracingEnabled option

- [x] 6.1 Added `WithTracingEnabled(v bool) Option` in `otelnats/conn.go` (`connConfig.TracingEnabled *bool`); `newConn` now resolves `cfg := newConnConfig(opts...)` before the enabled check (previously checked the env gate first and returned early, skipping option parsing entirely) — option present → authoritative, absent → existing `natsGate` value. All three `...WithOptions` connect variants funnel through the one `newConn`, so TLS/credentials are covered automatically. **No deliver-provider wiring needed**: verified during implementation that otel-nats currently has no deliver-span/OTLP-exporter mechanism at all (`initNATSProvider` referenced in CLAUDE.md/spec docs does not exist in the current codebase — pre-existing doc/code drift, out of scope here)
- [x] 6.2 Added whitebox tests (`otelnats/conn_option_test.go`): option-true overrides env-unset (→ `*tracedConn`), option-false overrides env-truthy (→ `*directConn`), option-absent matches the gate bit-for-bit in both directions
- [x] 6.3 Added `TestOteljetstreamInheritsConnTracingOption` (`oteljetstream/pushconsumer_test.go`) — proved `oteljetstream.New`'s existing `conn.TracingEnabled()` check already inherits the option correctly with zero oteljetstream-side code changes; this is also the first runtime test of the direct/untraced JetStream path in the package (previously blocked by the process-wide gate cache, per the NOTE it replaces)
- [x] 6.4 Full gate in `otel-nats/` — build, test -race (80 tests), lint all green

## 7. otel-mongo — R1 WithTracingEnabled (v1 + v2, parity)

- [x] 7.1 v1: added `WithTracingEnabled(v bool) ClientOption` in `otelmongo/client.go`; `ConnectWithOptions` restructured to resolve `cfg := newClientConfig(traceOpts)` and the effective `enabled` bool before branching (previously checked the env-only gate first and returned early, mirroring the same pre-existing early-branch pattern found and fixed in otel-nats). `Database`/`Collection` already inherit via cached fields (`d.tracingEnabled`, `newCollectionForDatabase`) — no changes needed there. **No deliver-provider wiring needed**: verified otel-mongo, like otel-nats, has no deliver-span/OTLP-exporter mechanism in the current codebase (`initMongoProvider` referenced in CLAUDE.md does not exist — same pre-existing doc/code drift, out of scope). **Bug caught by design, not by accident**: `resolveDocumentPropagation(override *bool)` internally recomputed the env-only `mongoTracingEnabled()` rather than accepting the caller's effective tracing state — so `WithTracingEnabled(true)` (env unset) combined with `WithTracePropagationEnabled(true)` would have silently stayed disabled. Fixed by reparameterizing to `resolveDocumentPropagation(tracingEnabled bool, override *bool)`; the process-wide env-only `propEnabledGate` (used by `ContextFromDocument`/`ContextFromRawDocument`) still passes the plain `mongoTracingEnabled()` explicitly, preserving its existing env-only behavior untouched
- [x] 7.2 v1 tests added (`otelmongo/client_option_test.go`, real container via existing `TestMain`/`requireMongoDB`): option-true+env-unset → traced impl; option-false+env-truthy → direct impl, propagation force-disabled despite `WithTracePropagationEnabled(true)`; **regression test for the propagation bug above** (option-true+env-unset+`WithTracePropagationEnabled(true)` → propagation actually enabled); option-absent parity both directions; `ContextFromDocument` proven env-only regardless of per-client option. Updated 4 existing `TestConnectPropagationResolution` subtests for the new `resolveDocumentPropagation` signature
- [x] 7.3 v2: mirrored 7.1–7.2 identically. Confirmed `env_flags_test.go`'s 4 pre-existing `resolveDocumentPropagation` call sites were byte-identical to v1's before the fix (diffed to verify), so copied v1's fixed file directly. `client_option_test.go` ported with v2-specific adjustments: `ConnectWithOptions` has no `ctx` parameter (v2 mongo-driver API difference, unrelated to this change), `v2/internal/{direct,traced}` import paths, `mongo-driver/v2/bson` — same 6 tests, same coverage
- [x] 7.4 Full gates in both `otel-mongo/` (138 tests) and `otel-mongo/v2/` (159 tests) — build, test -race, lint all green; confirmed `internal/direct` has no OTel SDK imports in either module (only a doc-comment mention, not an import); `examples/` (covers both v1 and v2 per CLAUDE.md) and both modules' `tests/integration/` build clean

## 8. otel-gorilla-ws — R1 WithTracingEnabled option

- [x] 8.1 Added `WithTracingEnabled(v bool) Option` (`connOptions.featureEnabled *bool`) in `options.go`; `applyOptions` overrides `c.featureEnabled` (the env-gate result, already set by `newConn` before `applyOptions` runs) before deciding the tracer. **Design landmine caught before implementing**: `Conn` has two similarly-named-but-distinct booleans — `featureEnabled` (env gate / OTel-SDK-code-path gate, what the option controls) and `tracingEnabled` (otel-ws subprotocol *negotiation outcome*, a per-connection fact the option must NOT touch — overriding it would corrupt wire-envelope expectations between client/server). Doc comment on `WithTracingEnabled` spells this out. **Pre-existing gap found and fixed as the correct mechanism**: `Upgrader.Upgrade` had no `opts ...Option` parameter at all (not even `WithTracerProvider`/`WithPropagators` could reach server-side connections) — added `opts ...Option` (variadic, backward-compatible with its intentional 3-arg mirror of `websocket.Upgrader.Upgrade`) and threaded it into `newConn`
- [x] 8.2 Added whitebox tests (`conn_option_test.go`): option overrides `featureEnabled` both directions (unit-level via `applyOptions` + a literal `&Conn{}`), full-stack proof through `NewConn`+real dial, and full-stack proof through `Upgrader.Upgrade`+real server; option-absent parity already covered by pre-existing `TestApplyOptions_GlobalFallback`/`TestApplyOptions_FeatureDisabled_UsesNoopTracer` (unchanged, still green)
- [x] 8.3 Full gate in `otel-gorilla-ws/` (51 tests) — build, test -race, lint all green; `examples/` and `tests/integration/` submodules verified to still build after the `Upgrade` signature change

## 9. D1 — CHANGELOG × 4 + VERSIONING.md

- [x] 9.1 Created `CHANGELOG.md` in each module directory. Went beyond a flat "0.6.0 backfill": researched actual release history (git tags, `git merge-base --is-ancestor` checks, `gh release list`) and found `otel-mongo`/`otel-mongo/v2` have an additional tagged-but-not-GitHub-released `0.6.1` (server.address per-command capture), and — more importantly — that commit `05247e4` ("remove deliver spans, fix span kinds and attributes") landed **after** every module's `v0.6.0` tag despite its commit message saying "(0.6.0)", so the deliver-span removal is actually unreleased on `main` right now. Each `[0.7.0] - Unreleased` entry combines that unreleased work with this change's own F1–F4/R5/F2/R1. `[0.6.0]` entries sourced from the accurate root `RELEASE-NOTES-0.6.0.md`. Pointer note explains the pre-0.6.0 gap (side-branch tagging, matches `VERSIONING.md`'s root-cause section)
- [x] 9.2 Created root `VERSIONING.md`: tag format, per-module independent versioning, 0.x policy (breaking → minor), release-notes locations (module CHANGELOG + GitHub Releases + optional root cross-module notes), version-constant location table (doubles as the D3 guard's reference), CI enforcement section. D2 (namespace retention) deliberately excluded per the 2026-07-10 decision — that's a personal GitHub-account action, not repo documentation
- [x] 9.3 Documented `WithTracingEnabled` in all three modules with a per-connection/client-option override paragraph (EN + zh-TW): otel-nats (README.md + README.zh-TW.md, plus the "Optional:" line and API summary table), otel-mongo (README.md + README.zh-TW.md, including the kill-switch/propagation-subordinate note), otel-gorilla-ws (README.md + README.zh-TW.md, including the `featureEnabled` vs `tracingEnabled` distinction — no separate options table exists in this module's README so the feature-flags section covers it). Also fixed the remaining F2 `messaging.consumer.name` → `messaging.consumer.group.name` references in otel-nats's two READMEs (found via a repo-wide grep sweep) and the Docker-gated `tests/integration/jetstream_test.go` assertion (verified that module still builds)

## 10. D3 — release-tag version guard

- [x] 10.1 Added `.github/workflows/release-guard.yml`, triggered on 4 explicit tag glob patterns (one per module — `otel-*/v*` alone does not match the nested `otel-mongo/v2/vX.Y.Z` shape since GitHub Actions tag globs don't cross `/`; verified by hand). Bash `case` parses the tag into module/version/file per `VERSIONING.md`'s table (`otel-mongo/v2` checked before plain `otel-mongo` so the more specific pattern wins), extracts the constant via `grep`+`sed`, fails with both values printed on mismatch
- [x] 10.2 **Bug caught by testing, not assumed correct**: dry-ran the extraction regex against all 4 real `version.go`/`conn.go` files before trusting it — `otel-mongo`/`otel-mongo/v2`'s `const instrumentationVersion = "..."` is a standalone statement with a `const` keyword prefix, unlike `otel-nats`'s block-style `instrumentationVersion = "..."` (no per-line prefix, inside a `const (...)` group); the original regex only matched the nats shape and silently extracted an empty string for both mongo modules. Fixed to `^\s*(const\s+)?instrumentationVersion\s*=`. Verified all 6 cases (match + mismatch × otel-nats/otel-mongo/otel-mongo-v2/otel-gorilla-ws) end-to-end by extracting the embedded script and running it directly with `GITHUB_REF_NAME` set, confirming correct exit codes (0 on match, 1 with a clear error message on mismatch); also validated the YAML itself parses

## 11. Release 0.7.0

- [x] 11.1 Bumped version constants to `0.7.0` in all four locations + updated `version_test.go` in all four modules. Swept for any other hardcoded `"0.6.2"`/`"0.6.0"` string literals in non-test and test `.go` files across all three module trees — none found beyond the version files themselves
- [x] 11.2 Updated `CLAUDE.md`: new "Per-connection tracing override (all four modules, 0.7.0+)" section (composes above `internal/flags`, no changes to that package; documents the mongo `resolveDocumentPropagation` pitfall and the gorilla-ws `featureEnabled` vs `tracingEnabled` distinction as landmines for future contributors); `MessageBatch.Stop()` section updated for F4 promptness; new "Receive-span lifecycle: end at handover" section for R5 + the `Consumer.Next` cancellation mechanism (F3); `otel-nats` module notes gained F1/F2 bullets; Versioning section now points to `VERSIONING.md`; CI section documents `release-guard.yml` including the regex-tolerance note for the two version-constant shapes
- [x] 11.3 Verified `internal/flags` — both `flags.go` and `flags_test.go`, excluding the `package` line — are byte-identical (matching `md5` hashes) across all four modules; unaffected by this change as expected, no edits made
- [x] 11.4 Full gates on all four modules after the version bump: **428 unit tests** (otel-nats 80, otel-mongo 138, otel-mongo/v2 159, otel-gorilla-ws 51), all `go build`/`golangci-lint` clean. **27 integration tests** (Docker, testcontainers) run and passed across all four `tests/integration/` sub-modules — otel-nats 11, otel-mongo 6, otel-mongo/v2 6, otel-gorilla-ws 4
- [x] 11.5 ~~Merge; tag; publish GitHub Releases~~ — **removed from this change** (2026-07-11, user decision): release execution happens **after** the change is archived (archive → merge → tag `otel-nats/v0.7.0` first so the guard self-validates, then the other three → GitHub Releases with migration notes). Not part of the change's implementation scope.
- [x] 11.6 ~~Update `UPSTREAM-ENGAGEMENT-NOTES.md`~~ — **obsolete**: the file was removed by the user (2026-07-11); no engagement-notes update to perform. Task closed as skipped.
