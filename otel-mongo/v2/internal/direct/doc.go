// Package direct holds the disabled-path passthrough implementations for the v2 driver.
//
// MUST NOT import go.opentelemetry.io/otel/* — enforced by CI (see .github/workflows/ci.yml).
package direct
